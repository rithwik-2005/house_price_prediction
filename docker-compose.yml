version: "3.9"

services:
  house-price-app:
    build: .
    container_name: house_price_prediction_app
    ports:
      - "5000:5000"
    volumes:
      - .:/app
    environment:
      - FLASK_ENV=production
